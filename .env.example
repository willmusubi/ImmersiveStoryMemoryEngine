# Immersive Story Memory Engine - 环境变量配置
# 复制此文件为 .env 并填入实际值

# ==================== OpenAI API 配置 ====================
# OpenAI API Key（必需）
# 从 https://platform.openai.com/api-keys 获取，或使用自定义 API
SUPER_MIND_API_KEY=your_api_key_here

# OpenAI API Base URL（可选）
# 默认：https://space.ai-builders.com/backend/v1
# 如果使用 OpenAI 官方 API，设置为：https://api.openai.com/v1
OPENAI_BASE_URL=https://space.ai-builders.com/backend/v1

# OpenAI 模型名称（可选）
# 默认：supermind-agent-v1
# 如果使用 OpenAI 官方 API，可以设置为：gpt-4, gpt-3.5-turbo 等
OPENAI_MODEL=supermind-agent-v1

# ==================== 数据库配置 ====================
# 数据库文件路径（可选）
# 默认：项目根目录/data/databases/memory_engine.db
# 使用绝对路径或相对路径均可
DB_PATH=data/databases/memory_engine.db

# ==================== RAG 索引配置 ====================
# RAG 索引基础目录（可选）
# 默认：项目根目录/data/indices
# 索引文件将保存在此目录下，格式：{story_id}_world_bible.index
RAG_INDEX_BASE_DIR=data/indices

# ==================== 故事配置 ====================
# 默认故事 ID（可选，用于测试）
# 如果不设置，需要在 API 调用时指定 story_id
STORY_ID=sanguo_test

# ==================== 其他配置 ====================
# 日志级别（可选）
# 可选值：DEBUG, INFO, WARNING, ERROR
# LOG_LEVEL=INFO
